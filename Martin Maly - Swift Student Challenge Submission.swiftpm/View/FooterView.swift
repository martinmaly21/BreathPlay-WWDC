import SwiftUI

struct FooterView: View {
    var body: some View {
        Text("A WWDC Swift Student Challenge submission by Martin Maly")
            .font(.system(.body, design: .monospaced))
    }
}
